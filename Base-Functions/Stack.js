/** Stacks with push and pop methods* */class Node {    constructor(val) {        this.val = val        this.next = null    }}class Stack {    constructor() {        this.first = null        this.last = null        this.size = 0    }    push(val) {        const node = new Node(val);        if (this.size === 0) {            this.first = node            this.last = node        } else {            this.last.next = node            this.last = node        }        this.size++        return this;    }    pop() {        if (this.size === 0) return null        const removedNode = this.last        if (this.size === 1) {            this.first = null;            this.last = null;        } else {            let currentNode = this.first;            let prev;            while (currentNode.next) {                prev = currentNode;                currentNode = currentNode.next;            }            this.last = prev;            this.last.next = null;        }        removedNode.next = null;        this.size--;        return removedNode;    }}const stack = new Stack();stack.push(1);stack.push(2);console.log(stack)stack.pop();console.log(stack)